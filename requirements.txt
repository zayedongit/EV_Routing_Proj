# Runtime dependencies.
#
# Versions are given as compatible ranges rather than exact pins: the original
# pins (ortools 9.8, numpy 1.24) have no wheels for Python 3.11+, so the project
# could not be installed on a current interpreter at all.  The lower bounds are
# the oldest versions the code is known to work with; the upper bounds guard
# against the next major release.
#
# The exact versions a benchmark ran under are recorded in
# results/environment.json, which is what makes a result reproducible.

ortools>=9.10,<10
numpy>=1.26,<3
pandas>=2.1,<3
matplotlib>=3.8,<4

# Dashboard
streamlit>=1.36,<2
plotly>=5.20,<7
pydeck>=0.9,<1
